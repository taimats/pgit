package cmd

var (
	InitCmd       = initCmd
	HashObjectCmd = hashObjCmd
	CatFileCmd    = catFileCmd
	WriteTreeCmd  = writeTreeCmd
	ReadTreeCmd   = readTreeCmd
	WriteTree     = writeTree
	CommitCmd     = commitCmd
	LogCmd        = logCmd
)
